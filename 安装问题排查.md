# CodexMeter 安装问题排查

## 问题现象

首次双击 `CodexMeter.app` 时，macOS 提示：

> Apple 无法验证“CodexMeter”是否包含可能危害 Mac 安全或泄露隐私的恶意软件。

这是 macOS Gatekeeper 的安全拦截，通常不是应用文件损坏。

## 原因

当前项目的构建脚本使用 ad-hoc 临时签名：

```bash
codesign --force --sign - CodexMeter.app
```

该签名仅适合本机开发和验证，未包含 Apple Developer ID 正式签名与 Apple 公证（Notarization）票据。应用从浏览器、聊天软件或压缩包下载后还会带有隔离属性，因此 macOS 会阻止直接打开。

## 自用安装方法

仅在确认应用来源可信时使用以下方法。

### 1. 将应用拖入“应用程序”目录

将 `CodexMeter.app` 拖到 Finder 的“应用程序”目录。完成后，应用通常位于：

```text
/Applications/CodexMeter.app
```

### 2. 移除下载隔离属性

在“终端”执行：

```bash
xattr -dr com.apple.quarantine "/Applications/CodexMeter.app"
```

然后重新双击打开应用。

### `No such file` 的处理

若出现以下错误：

```text
xattr: No such file: /Applications/CodexMeter.app
```

说明应用尚未被拖入“应用程序”目录，或其名称、位置不同。先查找实际位置：

```bash
mdfind 'kMDItemFSName == "CodexMeter.app"'
```

把查询结果完整替换到命令中。例如应用仍在下载目录：

```bash
xattr -dr com.apple.quarantine "$HOME/Downloads/CodexMeter.app"
```

路径包含空格时必须保留双引号。

### 不使用终端的方式

在 Finder 中选中 `CodexMeter.app`，按住 Control 键点击应用，选择“打开”，再在确认提示中选择“打开”。部分 macOS 版本也可在“系统设置 → 隐私与安全性”中选择仍要打开。

## 面向其他用户发布

不能仅依赖移除隔离属性。正式分发前需完成：

1. 使用 Apple Developer ID Application 证书签名应用；
2. 启用 Hardened Runtime；
3. 使用 `notarytool` 上传应用或 DMG 到 Apple 公证服务；
4. 公证成功后使用 `stapler` 将公证票据附加到发布产物；
5. 在未参与构建的 Mac 上下载并验证安装。

完成以上步骤后，用户通常可直接打开安装包，不需要执行 `xattr`。
